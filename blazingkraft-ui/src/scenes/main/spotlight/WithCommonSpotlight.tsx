import WithCommonSpotlightComponent from './WithCommonSpotlightComponent';

interface WithCommonSpotlightProps {
    children: React.ReactNode;
}

const WithCommonSpotlight = (props: WithCommonSpotlightProps) => {
    // Map State To Props

    // Map Dispatch To Props

    // Authorization

    return <WithCommonSpotlightComponent {...props} />;
};

export default WithCommonSpotlight;
